# バトルアリーナ v2 仕様書

オンライン2人対戦のブラウザ格闘ゲーム。サーバー権威型（物理・判定はすべてサーバーで処理）。

最終更新: 2026-05-30

---

## 1. 構成・技術

| 項目 | 内容 |
|------|------|
| サーバー | Python / Flask + Flask-SocketIO（gevent） |
| 物理ループ | サーバー側で60fps（`game_loop` デーモンスレッド） |
| クライアント | HTML5 Canvas（`templates/index.html` 単一ファイル、CSS/JSすべて内包） |
| 通信 | WebSocket（join, input, state, hit, ult_start, game_start, game_over, rematch ほか） |
| デプロイ | Render（GitHub `sayu182905/claude-game1` にpushで自動デプロイ） |
| ポート | `int(os.environ.get('PORT', 5000))`（Renderの`$PORT`対応、ローカルは5000） |

関連ドキュメント:
- パラメータ調整 → `PARAMETERS.md`（`app.py` の `CONFIG` と対応）
- キャラ追加手順 → `CHARACTERS.md`

---

## 2. ゲームの流れ

1. **タイトル画面**：キャラを選択（早い者勝ち）→ ルームIDを入れて「入室する」
2. **待機室**：相手の入室待ち（先に入った人が選んだキャラを確定、後の人は残りに）
3. **対戦**：両者揃うと自動開始。HPを0にした側の勝ち
4. **結果画面**：「もう一度」（両者押すと同ルーム再戦・再入室不要）／「タイトルへ」

---

## 3. 操作

| ボタン | 動作 |
|--------|------|
| ◀ ▶ | 左右移動（画面左下） |
| ↑ | ジャンプ（画面右側・弱/強の上段） |
| 🛡 | ガード（ダメージ1/3） |
| 弱 | 弱攻撃 |
| 強 | 強攻撃（クールダウンが弱の3倍） |
| 必殺 | 必殺技（ゲージMAX時のみ・画面右上に独立配置） |

- 画面は**横画面専用**。縦持ち時は「横にしてください」の案内を全画面表示
- スマホ横画面ではcanvasを全画面化し、HUD（上）と操作ボタン（下）を重ねて配置。ステージが画面の横幅いっぱいに広がる
- ボタン配置：**左下＝左右移動**／**右側＝上段にジャンプ・下段に🛡弱強**／**右上＝必殺**
- 攻撃時は四角ではなく**キャラ色の腕＋こぶし**が前方に出る

---

## 4. 戦闘ルール

- **攻撃判定**：横距離 `attack_range`(95px) 以内 **かつ** 縦の高低差 `attack_v_range`(55px) 以内のときヒット。空振りではゲージも溜まらない
- **ノックバック**：ダメージ量に比例（横 `×1.8`、上 `×0.7`）
- **必殺ゲージ**：相手にダメージを与えたときだけ増加（当てた側+5／被弾側+10）。**必殺発動中の本人は溜まらない**（多重発動防止）
- **The World停止中の被ダメージ**：ダメージは入るが、ノックバックはその場では発生せず、**停止解除の瞬間にまとめて吹っ飛ぶ**
- **ブラックホールに吸われている側は台に着地しない**（中央へ引き寄せる際に勝手に台へ上がらないようにするため）

---

## 5. キャラクター / 必殺技

| キャラ | 必殺技 | 効果 |
|--------|--------|------|
| 血まみれスライム | ブラックホール | 7秒間、相手だけをステージ中央へ引き寄せ＋継続ダメージ(1/秒)。**相手は左右移動もジャンプもできず抜け出せない** |
| かえる＆人間＆とけい | The World | 4秒間、相手を完全停止。**空中で発動した場合も、その瞬間の位置で固定**（落下せず静止）。停止中の被ダメージは解除時にまとめてノックバック |

---

## 6. 主要パラメータ（抜粋・現在値）

ステージ 800×300 / 地面y=240 / 重力0.5 / 最大HP100 / 移動速度4 / ジャンプ初速-11
弱5・強10ダメージ / ガード1/3 / 必殺コスト100

※全パラメータは `app.py` の `CONFIG` に集約。詳細・調整は `PARAMETERS.md` 参照。

---

## 7. 既知の仕様・トレードオフ

- 操作ボタンを大きめ（1.5倍）にしているため、左右の隅に押し込まれたキャラの足元はボタンに重なることがある（中央にいれば全身見える）
- 相手が切断すると部屋は閉じ、残った側には切断通知が出る（再戦不可）
